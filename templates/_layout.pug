block variables
  - let title = ""
  - let description = ""

doctype html
html(lang="ja")
  head
    title #{title}
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    block stylesheet
      link(rel="stylesheet"  type="text/css" href="./css/main.css")
    block javascript 
  body
    header
      #site_banner
        #site_icn_image
        #site_name Fuze666のホームページ
      #title_wrapper
        #title_color
        #title h1 #{title}
        nav#categoris
          ul
          li: a(href="/index.html") ホーム
          li: a(href="/games/games.html") ゲーム
      #description #{description}
    block main 
    #footer 
      ul: li something